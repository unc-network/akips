# Use dev machine

# Install poetry
poetry install

# Update docs
pdoc3 --html --output-dir docs akips --force

# Make sure you have rolled the version in pyproject.toml and __init__.py

# Run poetry tests, proceed if no errors
poetry run pytest -v -s tests

# Merge into master via pull request

# Publish to pypi
poetry build
poetry publish

# Create a tag for the version
